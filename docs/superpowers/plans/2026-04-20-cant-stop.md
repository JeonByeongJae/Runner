# Can't Stop Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 캔트스탑 보드게임을 1대1 온라인으로 플레이할 수 있는 React 앱을 구현한다.

**Architecture:** `src/games/cant-stop/` 하위에 타입·유틸·훅·화면을 배치한다. `useRoom`을 제네릭화하여 Firebase 구독을 재사용하고, `cantStopDb.ts`에서 캔트스탑 전용 Firebase 연산을 담당한다. GameScreen은 MountainBoard와 ActionPanel 컴포넌트로 분리한다.

**Tech Stack:** React 18, TypeScript, Firebase Realtime Database, Vite, Vitest, CSS Modules

---

## File Map

**수정:**
- `src/shared/hooks/useRoom.ts` — `useRoom<T>` 제네릭화 (subscribe 함수 주입)
- `src/games/runner/RunnerApp.tsx` — 제네릭 useRoom 호출로 변경
- `src/games/registry.ts` — cant-stop 항목 추가

**생성:**
- `src/games/cant-stop/types/index.ts` — 전체 타입 정의
- `src/games/cant-stop/utils/columns.ts` — 열별 칸 수, 승리 조건
- `src/games/cant-stop/utils/columns.test.ts`
- `src/games/cant-stop/utils/dice.ts` — 조합 계산, 가용 조합 확인
- `src/games/cant-stop/utils/dice.test.ts`
- `src/shared/firebase/cantStopDb.ts` — Firebase CRUD
- `src/games/cant-stop/hooks/useCantStopGame.ts` — 게임 상태 훅
- `src/games/cant-stop/screens/HomeScreen.tsx` + `.module.css`
- `src/games/cant-stop/screens/LobbyScreen.tsx` + `.module.css`
- `src/games/cant-stop/screens/GameScreen.tsx` + `.module.css`
- `src/games/cant-stop/components/MountainBoard.tsx` + `.module.css`
- `src/games/cant-stop/components/ActionPanel.tsx` + `.module.css`
- `src/games/cant-stop/screens/ResultScreen.tsx` + `.module.css`
- `src/games/cant-stop/CantStopApp.tsx`

---

## Task 1: 타입 정의

**Files:**
- Create: `src/games/cant-stop/types/index.ts`

- [ ] **Step 1: 타입 파일 생성**

```typescript
// src/games/cant-stop/types/index.ts

export type PlayerKey = 'host' | 'guest'

export interface CantStopPlayer {
  name: string
}

export interface ColumnState {
  host: number    // 확정 캠프 위치 (0 = 없음)
  guest: number
  locked: PlayerKey | null  // 점령한 플레이어, null = 미점령
}

export interface CantStopRoomState {
  status: 'waiting' | 'playing' | 'finished'
  players: {
    host?: CantStopPlayer
    guest?: CantStopPlayer
  }
  turn: PlayerKey | null
  board: Record<string, ColumnState>   // "2" ~ "12"
  climbers: Record<string, number>     // "2" ~ "12", 임시 등반자 위치
  dice: number[]                       // 굴린 주사위 4개
  rolledThisTurn: boolean
  winner: PlayerKey | null
}

export interface Session {
  roomId: string
  myKey: PlayerKey
  myName: string
}
```

- [ ] **Step 2: 커밋**

```bash
git add src/games/cant-stop/types/index.ts
git commit -m "feat(cant-stop): 타입 정의 추가"
```

---

## Task 2: Column 유틸

**Files:**
- Create: `src/games/cant-stop/utils/columns.ts`
- Create: `src/games/cant-stop/utils/columns.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

```typescript
// src/games/cant-stop/utils/columns.test.ts
import { describe, it, expect } from 'vitest'
import { COLS, getColumnSize, isVictory, initBoard } from './columns'

describe('COLS', () => {
  it('2열은 3칸', () => expect(COLS[2]).toBe(3))
  it('7열은 13칸', () => expect(COLS[7]).toBe(13))
  it('12열은 3칸', () => expect(COLS[12]).toBe(3))
})

describe('getColumnSize', () => {
  it('숫자로 칸 수를 반환한다', () => expect(getColumnSize(7)).toBe(13))
})

describe('isVictory', () => {
  it('잠긴 열이 3개 미만이면 false', () => {
    const board = initBoard()
    board['7'].locked = 'host'
    board['8'].locked = 'host'
    expect(isVictory(board, 'host')).toBe(false)
  })

  it('잠긴 열이 3개이면 true', () => {
    const board = initBoard()
    board['7'].locked = 'host'
    board['8'].locked = 'host'
    board['9'].locked = 'host'
    expect(isVictory(board, 'host')).toBe(true)
  })

  it('상대방 잠금은 카운트 안 함', () => {
    const board = initBoard()
    board['7'].locked = 'guest'
    board['8'].locked = 'guest'
    board['9'].locked = 'guest'
    expect(isVictory(board, 'host')).toBe(false)
  })
})
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```bash
cd /Users/user/Runner && npx vitest run src/games/cant-stop/utils/columns.test.ts
```

Expected: FAIL (모듈 없음)

- [ ] **Step 3: 구현 작성**

```typescript
// src/games/cant-stop/utils/columns.ts
import type { ColumnState, PlayerKey } from '../types'

export const COLS: Record<number, number> = {
  2: 3, 3: 5, 4: 7, 5: 9, 6: 11,
  7: 13, 8: 11, 9: 9, 10: 7, 11: 5, 12: 3,
}

export function getColumnSize(col: number): number {
  return COLS[col]
}

export function initBoard(): Record<string, ColumnState> {
  const board: Record<string, ColumnState> = {}
  for (let col = 2; col <= 12; col++) {
    board[String(col)] = { host: 0, guest: 0, locked: null }
  }
  return board
}

export function isVictory(
  board: Record<string, ColumnState>,
  player: PlayerKey
): boolean {
  return Object.values(board).filter(c => c.locked === player).length >= 3
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
cd /Users/user/Runner && npx vitest run src/games/cant-stop/utils/columns.test.ts
```

Expected: PASS (6 tests)

- [ ] **Step 5: 커밋**

```bash
git add src/games/cant-stop/utils/columns.ts src/games/cant-stop/utils/columns.test.ts
git commit -m "feat(cant-stop): column 유틸 추가"
```

---

## Task 3: Dice 유틸

**Files:**
- Create: `src/games/cant-stop/utils/dice.ts`
- Create: `src/games/cant-stop/utils/dice.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

```typescript
// src/games/cant-stop/utils/dice.test.ts
import { describe, it, expect } from 'vitest'
import { rollDice, getDiceCombos, isComboPlayable } from './dice'
import type { ColumnState } from '../types'

describe('rollDice', () => {
  it('4개의 1~6 숫자를 반환한다', () => {
    const dice = rollDice()
    expect(dice).toHaveLength(4)
    dice.forEach(d => {
      expect(d).toBeGreaterThanOrEqual(1)
      expect(d).toBeLessThanOrEqual(6)
    })
  })
})

describe('getDiceCombos', () => {
  it('[1,2,3,4]로 3가지 조합을 반환한다', () => {
    const combos = getDiceCombos([1, 2, 3, 4])
    expect(combos).toHaveLength(3)
    expect(combos[0]).toEqual([3, 7])  // 1+2, 3+4
    expect(combos[1]).toEqual([4, 6])  // 1+3, 2+4
    expect(combos[2]).toEqual([5, 5])  // 1+4, 2+3
  })
})

describe('isComboPlayable', () => {
  const makeBoard = (locked?: string): Record<string, ColumnState> => {
    const board: Record<string, ColumnState> = {}
    for (let c = 2; c <= 12; c++) {
      board[String(c)] = { host: 0, guest: 0, locked: locked === String(c) ? 'guest' : null }
    }
    return board
  }

  it('등반자가 비어있으면 새 등반자 배치 가능 (최대 3개 미만)', () => {
    const board = makeBoard()
    const climbers = { '7': 5 }
    expect(isComboPlayable([7, 8], board, climbers, 'host')).toBe(true)
  })

  it('등반자 3개이고 조합이 기존 열에 없으면 false', () => {
    const board = makeBoard()
    const climbers = { '7': 5, '8': 3, '9': 2 }
    expect(isComboPlayable([2, 3], board, climbers, 'host')).toBe(false)
  })

  it('잠긴 열은 사용 불가', () => {
    const board = makeBoard()
    board['7'].locked = 'guest'
    const climbers = {}
    expect(isComboPlayable([7, 5], board, climbers, 'host')).toBe(false)
  })

  it('잠긴 열을 제외하고 다른 열이 가능하면 true', () => {
    const board = makeBoard()
    board['7'].locked = 'guest'
    const climbers = {}
    expect(isComboPlayable([7, 5], board, climbers, 'guest')).toBe(false) // 7도 잠겼고 guest가 잠금
    expect(isComboPlayable([5, 6], board, climbers, 'host')).toBe(true)
  })
})
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```bash
cd /Users/user/Runner && npx vitest run src/games/cant-stop/utils/dice.test.ts
```

Expected: FAIL (모듈 없음)

- [ ] **Step 3: 구현 작성**

```typescript
// src/games/cant-stop/utils/dice.ts
import type { ColumnState, PlayerKey } from '../types'
import { COLS } from './columns'

export function rollDice(): number[] {
  return Array.from({ length: 4 }, () => Math.floor(Math.random() * 6) + 1)
}

export function getDiceCombos(dice: number[]): [number, number][] {
  const [d1, d2, d3, d4] = dice
  return [
    [d1 + d2, d3 + d4],
    [d1 + d3, d2 + d4],
    [d1 + d4, d2 + d3],
  ]
}

export function isComboPlayable(
  combo: [number, number],
  board: Record<string, ColumnState>,
  climbers: Record<string, number>,
  player: PlayerKey
): boolean {
  const climberCount = Object.keys(climbers).length
  return combo.some(col => {
    if (!COLS[col]) return false
    const colState = board[String(col)]
    if (colState?.locked !== null && colState?.locked !== undefined) return false
    if (climbers[String(col)] !== undefined) return true
    return climberCount < 3
  })
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
cd /Users/user/Runner && npx vitest run src/games/cant-stop/utils/dice.test.ts
```

Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add src/games/cant-stop/utils/dice.ts src/games/cant-stop/utils/dice.test.ts
git commit -m "feat(cant-stop): dice 유틸 추가"
```

---

## Task 4: useRoom 제네릭화

**Files:**
- Modify: `src/shared/hooks/useRoom.ts`
- Modify: `src/games/runner/RunnerApp.tsx`

- [ ] **Step 1: useRoom 제네릭으로 수정**

```typescript
// src/shared/hooks/useRoom.ts
import { useState, useEffect } from 'react'

export function useRoom<T>(
  roomId: string | null,
  subscribe: (roomId: string, cb: (data: T | null) => void) => () => void
) {
  const [room, setRoom] = useState<T | null>(null)
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    if (!roomId) {
      setRoom(null)
      setLoading(false)
      return
    }
    setLoading(true)
    const unsubscribe = subscribe(roomId, data => {
      setRoom(data)
      setLoading(false)
    })
    return unsubscribe
  }, [roomId, subscribe])

  return { room, loading }
}
```

- [ ] **Step 2: RunnerApp.tsx 호출 변경**

`src/games/runner/RunnerApp.tsx`에서 useRoom 호출 부분을 찾아 수정:

```typescript
// 기존
import { useRoom } from '../../shared/hooks/useRoom'
// 추가
import { subscribeRoom } from '../../shared/firebase/roomDb'
import type { GameRoom } from './types/game'

// 기존
const { room, loading } = useRoom(session?.roomId ?? null)
// 변경
const { room, loading } = useRoom<GameRoom>(session?.roomId ?? null, subscribeRoom)
```

- [ ] **Step 3: 빌드 확인**

```bash
cd /Users/user/Runner && npm run build 2>&1 | tail -20
```

Expected: 에러 없음

- [ ] **Step 4: 커밋**

```bash
git add src/shared/hooks/useRoom.ts src/games/runner/RunnerApp.tsx
git commit -m "refactor: useRoom 제네릭화"
```

---

## Task 5: Firebase DB 함수 (cantStopDb)

**Files:**
- Create: `src/shared/firebase/cantStopDb.ts`

- [ ] **Step 1: cantStopDb 작성**

```typescript
// src/shared/firebase/cantStopDb.ts
import { ref, set, get, update, onValue, off } from 'firebase/database'
import { db } from './config'
import type { CantStopRoomState, PlayerKey } from '../../games/cant-stop/types'
import { initBoard } from '../../games/cant-stop/utils/columns'
import { rollDice } from '../../games/cant-stop/utils/dice'
import { isVictory } from '../../games/cant-stop/utils/columns'

function generateRoomId(): string {
  return Math.random().toString(36).slice(2, 8).toUpperCase()
}

export async function createRoom(hostName: string): Promise<string> {
  const roomId = generateRoomId()
  const state: CantStopRoomState = {
    status: 'waiting',
    players: { host: { name: hostName } },
    turn: null,
    board: initBoard(),
    climbers: {},
    dice: [],
    rolledThisTurn: false,
    winner: null,
  }
  await set(ref(db, `rooms/cant-stop/${roomId}`), state)
  return roomId
}

export async function joinRoom(roomId: string, guestName: string): Promise<void> {
  const snap = await get(ref(db, `rooms/cant-stop/${roomId}`))
  if (!snap.exists()) throw new Error('방을 찾을 수 없습니다.')
  const room = snap.val() as CantStopRoomState
  if (room.status !== 'waiting') throw new Error('이미 시작된 방입니다.')

  await update(ref(db, `rooms/cant-stop/${roomId}`), {
    'players/guest': { name: guestName },
    status: 'playing',
    turn: 'host',
  })
}

export async function rollDiceAction(roomId: string): Promise<void> {
  const dice = rollDice()
  await update(ref(db, `rooms/cant-stop/${roomId}`), {
    dice,
    rolledThisTurn: true,
  })
}

export async function applyCombo(
  roomId: string,
  combo: [number, number]
): Promise<void> {
  const snap = await get(ref(db, `rooms/cant-stop/${roomId}`))
  const room = snap.val() as CantStopRoomState
  const climbers = { ...(room.climbers ?? {}) }

  for (const col of combo) {
    const key = String(col)
    const colState = room.board?.[key]
    if (!colState || colState.locked !== null) continue
    const currentPos = climbers[key] ?? (colState[room.turn as PlayerKey] ?? 0)
    climbers[key] = currentPos + 1
  }

  await update(ref(db, `rooms/cant-stop/${roomId}`), { climbers })
}

export async function stopClimbing(roomId: string): Promise<void> {
  const snap = await get(ref(db, `rooms/cant-stop/${roomId}`))
  const room = snap.val() as CantStopRoomState
  const player = room.turn as PlayerKey
  const board = JSON.parse(JSON.stringify(room.board)) as Record<string, { host: number; guest: number; locked: string | null }>

  for (const [key, pos] of Object.entries(room.climbers ?? {})) {
    const col = Number(key)
    board[key][player] = pos
    const { COLS } = await import('../../games/cant-stop/utils/columns')
    if (pos >= COLS[col]) {
      board[key].locked = player
    }
  }

  const victory = isVictory(board as Parameters<typeof isVictory>[0], player)
  const nextTurn: PlayerKey = player === 'host' ? 'guest' : 'host'

  await update(ref(db, `rooms/cant-stop/${roomId}`), {
    board,
    climbers: {},
    dice: [],
    rolledThisTurn: false,
    turn: victory ? null : nextTurn,
    status: victory ? 'finished' : 'playing',
    winner: victory ? player : null,
  })
}

export async function bust(roomId: string): Promise<void> {
  const snap = await get(ref(db, `rooms/cant-stop/${roomId}`))
  const room = snap.val() as CantStopRoomState
  const nextTurn: PlayerKey = room.turn === 'host' ? 'guest' : 'host'

  await update(ref(db, `rooms/cant-stop/${roomId}`), {
    climbers: {},
    dice: [],
    rolledThisTurn: false,
    turn: nextTurn,
  })
}

export function subscribeRoom(
  roomId: string,
  callback: (room: CantStopRoomState | null) => void
): () => void {
  const roomRef = ref(db, `rooms/cant-stop/${roomId}`)
  onValue(roomRef, snap => {
    callback(snap.exists() ? (snap.val() as CantStopRoomState) : null)
  })
  return () => off(roomRef)
}
```

- [ ] **Step 2: 빌드 확인**

```bash
cd /Users/user/Runner && npm run build 2>&1 | tail -20
```

Expected: 에러 없음

- [ ] **Step 3: 커밋**

```bash
git add src/shared/firebase/cantStopDb.ts
git commit -m "feat(cant-stop): Firebase DB 함수 추가"
```

---

## Task 6: useCantStopGame 훅

**Files:**
- Create: `src/games/cant-stop/hooks/useCantStopGame.ts`

- [ ] **Step 1: 훅 작성**

```typescript
// src/games/cant-stop/hooks/useCantStopGame.ts
import { useCallback } from 'react'
import { useRoom } from '../../../shared/hooks/useRoom'
import {
  subscribeRoom,
  rollDiceAction,
  applyCombo,
  stopClimbing,
  bust,
} from '../../../shared/firebase/cantStopDb'
import type { CantStopRoomState, PlayerKey } from '../types'
import { getDiceCombos, isComboPlayable } from '../utils/dice'

export function useCantStopGame(roomId: string | null, myKey: PlayerKey) {
  const { room, loading } = useRoom<CantStopRoomState>(roomId, subscribeRoom)

  const isMyTurn = room?.turn === myKey

  const combos = room?.dice.length === 4
    ? getDiceCombos(room.dice)
    : []

  const comboPlayable = combos.map(combo =>
    room
      ? isComboPlayable(combo, room.board, room.climbers ?? {}, myKey)
      : false
  )

  const hasPlayableCombo = comboPlayable.some(Boolean)

  const handleRoll = useCallback(async () => {
    if (!roomId || !isMyTurn) return
    await rollDiceAction(roomId)
  }, [roomId, isMyTurn])

  const handleSelectCombo = useCallback(async (idx: number) => {
    if (!roomId || !isMyTurn || !room) return
    await applyCombo(roomId, combos[idx])
  }, [roomId, isMyTurn, room, combos])

  const handleStop = useCallback(async () => {
    if (!roomId || !isMyTurn) return
    await stopClimbing(roomId)
  }, [roomId, isMyTurn])

  const handleBust = useCallback(async () => {
    if (!roomId) return
    await bust(roomId)
  }, [roomId])

  return {
    room,
    loading,
    isMyTurn,
    combos,
    comboPlayable,
    hasPlayableCombo,
    handleRoll,
    handleSelectCombo,
    handleStop,
    handleBust,
  }
}
```

- [ ] **Step 2: 빌드 확인**

```bash
cd /Users/user/Runner && npm run build 2>&1 | tail -20
```

Expected: 에러 없음

- [ ] **Step 3: 커밋**

```bash
git add src/games/cant-stop/hooks/useCantStopGame.ts
git commit -m "feat(cant-stop): useCantStopGame 훅 추가"
```

---

## Task 7: HomeScreen

**Files:**
- Create: `src/games/cant-stop/screens/HomeScreen.tsx`
- Create: `src/games/cant-stop/screens/HomeScreen.module.css`

- [ ] **Step 1: CSS 작성**

```css
/* src/games/cant-stop/screens/HomeScreen.module.css */
.screen {
  min-height: 100vh;
  background: var(--color-bg, #2c1810);
  display: flex; align-items: center; justify-content: center;
  padding: 24px;
}
.card {
  background: var(--color-surface, #1a0e08);
  border: 2px solid #8a6a30;
  border-radius: 16px;
  padding: 32px 24px;
  width: 100%; max-width: 360px;
  display: flex; flex-direction: column; gap: 14px;
}
.title {
  font-size: 28px; font-weight: bold;
  color: #c8a45a; text-align: center; letter-spacing: 2px;
}
.subtitle {
  font-size: 14px; color: #a08060; text-align: center; margin-top: -8px;
}
.input {
  background: #2c1810; border: 1.5px solid #8a6a30; border-radius: 8px;
  color: #f5e6c8; font-size: 16px; padding: 10px 14px; width: 100%;
}
.input:focus { outline: none; border-color: #c8a45a; }
.primaryBtn {
  background: #3a2010; border: 1.5px solid #c8a45a; border-radius: 10px;
  color: #c8a45a; font-size: 15px; font-weight: bold;
  padding: 12px 0; cursor: pointer; width: 100%;
}
.primaryBtn:disabled { opacity: 0.4; cursor: default; }
.divider {
  text-align: center; color: #a08060; font-size: 13px;
}
.joinRow { display: flex; gap: 8px; }
.codeInput {
  flex: 1; background: #2c1810; border: 1.5px solid #8a6a30; border-radius: 8px;
  color: #f5e6c8; font-size: 16px; padding: 10px 14px;
  text-transform: uppercase;
}
.codeInput:focus { outline: none; border-color: #c8a45a; }
.joinBtn {
  background: #2c1810; border: 1.5px solid #8a6a30; border-radius: 8px;
  color: #f5e6c8; font-size: 15px; font-weight: bold;
  padding: 10px 20px; cursor: pointer;
}
.joinBtn:disabled { opacity: 0.4; cursor: default; }
.error { color: #e06060; font-size: 13px; text-align: center; }
```

- [ ] **Step 2: 컴포넌트 작성**

```tsx
// src/games/cant-stop/screens/HomeScreen.tsx
import { useState } from 'react'
import { createRoom, joinRoom } from '../../../shared/firebase/cantStopDb'
import type { PlayerKey } from '../types'
import styles from './HomeScreen.module.css'

interface Props {
  onEnterRoom: (roomId: string, myKey: PlayerKey, myName: string) => void
}

export default function HomeScreen({ onEnterRoom }: Props) {
  const [name, setName] = useState('')
  const [joinCode, setJoinCode] = useState('')
  const [error, setError] = useState('')
  const [loading, setLoading] = useState(false)

  const handleCreate = async () => {
    if (!name.trim()) return setError('닉네임을 입력하세요')
    setLoading(true)
    try {
      const roomId = await createRoom(name.trim())
      onEnterRoom(roomId, 'host', name.trim())
    } catch {
      setError('방 생성에 실패했습니다')
    } finally {
      setLoading(false)
    }
  }

  const handleJoin = async () => {
    if (!name.trim()) return setError('닉네임을 입력하세요')
    if (!joinCode.trim()) return setError('방 코드를 입력하세요')
    setLoading(true)
    try {
      const code = joinCode.trim().toUpperCase()
      await joinRoom(code, name.trim())
      onEnterRoom(code, 'guest', name.trim())
    } catch (e) {
      setError(e instanceof Error ? e.message : '방 참가에 실패했습니다')
    } finally {
      setLoading(false)
    }
  }

  return (
    <div className={styles.screen}>
      <div className={styles.card}>
        <h1 className={styles.title}>CAN'T STOP</h1>
        <p className={styles.subtitle}>캔트스탑</p>

        <input
          className={styles.input}
          placeholder="닉네임"
          value={name}
          onChange={e => { setName(e.target.value); setError('') }}
          maxLength={12}
        />

        <button className={styles.primaryBtn} onClick={handleCreate} disabled={loading}>
          방 만들기
        </button>

        <div className={styles.divider}>또는</div>

        <div className={styles.joinRow}>
          <input
            className={styles.codeInput}
            placeholder="방 코드"
            value={joinCode}
            onChange={e => { setJoinCode(e.target.value.toUpperCase()); setError('') }}
            maxLength={6}
          />
          <button className={styles.joinBtn} onClick={handleJoin} disabled={loading}>
            참가
          </button>
        </div>

        {error && <p className={styles.error}>{error}</p>}
      </div>
    </div>
  )
}
```

- [ ] **Step 3: 빌드 확인**

```bash
cd /Users/user/Runner && npm run build 2>&1 | tail -20
```

Expected: 에러 없음

- [ ] **Step 4: 커밋**

```bash
git add src/games/cant-stop/screens/HomeScreen.tsx src/games/cant-stop/screens/HomeScreen.module.css
git commit -m "feat(cant-stop): HomeScreen 추가"
```

---

## Task 8: LobbyScreen

**Files:**
- Create: `src/games/cant-stop/screens/LobbyScreen.tsx`
- Create: `src/games/cant-stop/screens/LobbyScreen.module.css`

- [ ] **Step 1: CSS 작성**

```css
/* src/games/cant-stop/screens/LobbyScreen.module.css */
.screen {
  min-height: 100vh;
  background: #2c1810;
  display: flex; align-items: center; justify-content: center;
  padding: 24px;
}
.card {
  background: #1a0e08;
  border: 2px solid #8a6a30;
  border-radius: 16px;
  padding: 32px 24px;
  width: 100%; max-width: 360px;
  display: flex; flex-direction: column; gap: 20px;
  text-align: center;
}
.title { font-size: 22px; font-weight: bold; color: #c8a45a; }
.codeLabel { font-size: 13px; color: #a08060; }
.code {
  font-size: 36px; font-weight: bold; color: #f5e6c8;
  letter-spacing: 8px; font-family: monospace;
}
.waiting { font-size: 14px; color: #a08060; }
.playerList { display: flex; flex-direction: column; gap: 8px; }
.player {
  background: #2c1810; border: 1px solid #3d2415;
  border-radius: 8px; padding: 10px 16px;
  display: flex; justify-content: space-between; align-items: center;
  color: #f5e6c8; font-size: 15px;
}
.playerReady { color: #c8a45a; font-size: 12px; }
```

- [ ] **Step 2: 컴포넌트 작성**

```tsx
// src/games/cant-stop/screens/LobbyScreen.tsx
import type { CantStopRoomState, PlayerKey } from '../types'
import styles from './LobbyScreen.module.css'

interface Props {
  room: CantStopRoomState
  roomId: string
  myKey: PlayerKey
}

export default function LobbyScreen({ room, roomId }: Props) {
  return (
    <div className={styles.screen}>
      <div className={styles.card}>
        <h2 className={styles.title}>⛰️ CAN'T STOP</h2>

        <div>
          <p className={styles.codeLabel}>방 코드</p>
          <p className={styles.code}>{roomId}</p>
        </div>

        <div className={styles.playerList}>
          <div className={styles.player}>
            <span>{room.players.host?.name ?? '—'}</span>
            <span className={styles.playerReady}>방장</span>
          </div>
          <div className={styles.player}>
            <span>{room.players.guest?.name ?? '—'}</span>
            {!room.players.guest && (
              <span className={styles.waiting}>대기 중...</span>
            )}
          </div>
        </div>

        <p className={styles.waiting}>
          {room.players.guest ? '게임 시작 중...' : '상대방을 기다리는 중...'}
        </p>
      </div>
    </div>
  )
}
```

- [ ] **Step 3: 커밋**

```bash
git add src/games/cant-stop/screens/LobbyScreen.tsx src/games/cant-stop/screens/LobbyScreen.module.css
git commit -m "feat(cant-stop): LobbyScreen 추가"
```

---

## Task 9: MountainBoard 컴포넌트

**Files:**
- Create: `src/games/cant-stop/components/MountainBoard.tsx`
- Create: `src/games/cant-stop/components/MountainBoard.module.css`

- [ ] **Step 1: CSS 작성**

```css
/* src/games/cant-stop/components/MountainBoard.module.css */
.board {
  flex: 1;
  background: #1a0e08;
  padding: 16px 4px 10px;
  display: flex;
  align-items: flex-end;
  justify-content: center;
  gap: 3px;
  overflow-x: auto;
}
.column { display: flex; flex-direction: column; align-items: center; gap: 2px; }
.colLabel { font-size: 11px; color: #a08060; margin-top: 5px; font-weight: bold; }
.colLabelActive { color: #c8a45a; }
.cell {
  width: 30px; height: 30px;
  border-radius: 4px;
  background: #2c1810;
  border: 1px solid #3d2415;
  display: flex; align-items: center; justify-content: center;
  flex-shrink: 0;
}
.cellTop { border-radius: 6px 6px 4px 4px; }
.cellClimber { background: #3a2a10; border-color: #c8a45a; box-shadow: 0 0 6px #c8a45a88; }
.cellMyBase  { background: #1d3a1d; border-color: #3db83d; }
.cellOppBase { background: #3a1a1a; border-color: #cc3333; }
.cellLocked  { opacity: 0.5; }
```

- [ ] **Step 2: 컴포넌트 작성**

```tsx
// src/games/cant-stop/components/MountainBoard.tsx
import { COLS } from '../utils/columns'
import type { CantStopRoomState, PlayerKey } from '../types'
import styles from './MountainBoard.module.css'

function PickaxeSVG() {
  return (
    <svg viewBox="0 0 24 24" fill="none" width={22} height={22}>
      <line x1="5" y1="20" x2="17" y2="8" stroke="#a07830" strokeWidth="2.2" strokeLinecap="round"/>
      <path d="M9,5 C15,3 22,7 20,13 L18,11 C20,8 15,5 11,6 Z" fill="#c8a45a"/>
    </svg>
  )
}

function TentSVG({ color }: { color: 'green' | 'red' }) {
  const fill = color === 'green' ? '#1d6b1d' : '#6b1d1d'
  const stroke = color === 'green' ? '#3db83d' : '#cc3333'
  const inner = color === 'green' ? '#0d4a0d' : '#4a0d0d'
  return (
    <svg viewBox="0 0 24 24" fill="none" width={22} height={22}>
      <polygon points="12,4 3,20 21,20" fill={fill} stroke={stroke} strokeWidth="1.5" strokeLinejoin="round"/>
      <polygon points="12,4 7,20 17,20" fill={inner} stroke={stroke} strokeWidth="0.8"/>
      <rect x="9.5" y="14" width="5" height="6" rx="1.5" fill={stroke}/>
    </svg>
  )
}

interface Props {
  room: CantStopRoomState
  myKey: PlayerKey
}

export default function MountainBoard({ room, myKey }: Props) {
  const oppKey: PlayerKey = myKey === 'host' ? 'guest' : 'host'

  return (
    <div className={styles.board}>
      {Object.entries(COLS).map(([colNum, size]) => {
        const col = Number(colNum)
        const key = String(col)
        const colState = room.board[key]
        const hasClimber = room.climbers?.[key] !== undefined
        const isActive = hasClimber

        return (
          <div key={col} className={styles.column}>
            {Array.from({ length: size }, (_, i) => {
              const pos = size - i  // top = size, bottom = 1
              const isTop = pos === size
              const climberPos = room.climbers?.[key]
              const myBasePos = colState?.[myKey] ?? 0
              const oppBasePos = colState?.[oppKey] ?? 0
              const locked = colState?.locked

              const isClimber = climberPos === pos
              const isMyBase = myBasePos === pos
              const isOppBase = oppBasePos === pos

              let cellClass = styles.cell
              if (isTop) cellClass += ` ${styles.cellTop}`
              if (isClimber) cellClass += ` ${styles.cellClimber}`
              else if (isMyBase) cellClass += ` ${styles.cellMyBase}`
              else if (isOppBase) cellClass += ` ${styles.cellOppBase}`
              if (locked) cellClass += ` ${styles.cellLocked}`

              return (
                <div key={pos} className={cellClass}>
                  {isClimber && <PickaxeSVG />}
                  {!isClimber && isMyBase && <TentSVG color="green" />}
                  {!isClimber && !isMyBase && isOppBase && <TentSVG color="red" />}
                </div>
              )
            })}
            <div className={`${styles.colLabel}${isActive ? ` ${styles.colLabelActive}` : ''}`}>
              {col}
            </div>
          </div>
        )
      })}
    </div>
  )
}
```

- [ ] **Step 3: 빌드 확인**

```bash
cd /Users/user/Runner && npm run build 2>&1 | tail -20
```

Expected: 에러 없음

- [ ] **Step 4: 커밋**

```bash
git add src/games/cant-stop/components/MountainBoard.tsx src/games/cant-stop/components/MountainBoard.module.css
git commit -m "feat(cant-stop): MountainBoard 컴포넌트 추가"
```

---

## Task 10: ActionPanel 컴포넌트

**Files:**
- Create: `src/games/cant-stop/components/ActionPanel.tsx`
- Create: `src/games/cant-stop/components/ActionPanel.module.css`

- [ ] **Step 1: CSS 작성**

```css
/* src/games/cant-stop/components/ActionPanel.module.css */
.panel {
  background: #1a0e08;
  border-top: 2px solid #8a6a30;
  padding: 14px 16px;
  display: flex; flex-direction: column; gap: 12px;
}
.diceRow { display: flex; gap: 10px; justify-content: center; }
.die {
  width: 48px; height: 48px;
  background: #5c3a1e; border: 2px solid #8a6a30; border-radius: 10px;
  display: flex; align-items: center; justify-content: center;
  font-size: 24px; font-weight: bold; color: #f5e6c8;
}
.combos { display: flex; gap: 8px; justify-content: center; flex-wrap: wrap; }
.comboCard {
  background: #2c1810; border: 2px solid #555;
  border-radius: 10px; padding: 8px 16px; text-align: center; cursor: pointer;
  transition: border-color 0.1s;
}
.comboCard:hover:not(.disabled) { border-color: #8a6a30; }
.comboCardSelected { border-color: #c8a45a !important; background: #3a2010; }
.comboCardDisabled { opacity: 0.3; cursor: default; }
.comboSum { font-size: 18px; font-weight: bold; color: #f5e6c8; }
.comboSumSelected { color: #c8a45a; }
.comboDetail { font-size: 11px; color: #a08060; margin-top: 2px; }
.btnRow { display: flex; gap: 10px; }
.btn {
  flex: 1; padding: 12px 0; border-radius: 10px;
  border: none; font-size: 15px; font-weight: bold; cursor: pointer;
}
.btn:disabled { opacity: 0.4; cursor: default; }
.btnStop { background: #1d4a1d; color: #6ddf6d; border: 1.5px solid #3db83d; }
.btnRoll { background: #4a1a1a; color: #df6d6d; border: 1.5px solid #cc3333; }
.waitMsg { text-align: center; color: #a08060; font-size: 14px; padding: 8px 0; }
```

- [ ] **Step 2: 컴포넌트 작성**

```tsx
// src/games/cant-stop/components/ActionPanel.tsx
import { useState } from 'react'
import type { CantStopRoomState } from '../types'
import { getDiceCombos } from '../utils/dice'
import styles from './ActionPanel.module.css'

interface Props {
  room: CantStopRoomState
  isMyTurn: boolean
  comboPlayable: boolean[]
  onRoll: () => void
  onSelectCombo: (idx: number) => void
  onStop: () => void
}

export default function ActionPanel({
  room, isMyTurn, comboPlayable, onRoll, onSelectCombo, onStop,
}: Props) {
  const [selectedCombo, setSelectedCombo] = useState<number | null>(null)
  const combos = room.dice.length === 4 ? getDiceCombos(room.dice) : []
  const climberCount = Object.keys(room.climbers ?? {}).length

  if (!isMyTurn) {
    return (
      <div className={styles.panel}>
        <p className={styles.waitMsg}>상대방 차례입니다...</p>
      </div>
    )
  }

  const handleComboClick = (idx: number) => {
    if (!comboPlayable[idx]) return
    setSelectedCombo(idx)
    onSelectCombo(idx)
  }

  const handleRoll = () => {
    setSelectedCombo(null)
    onRoll()
  }

  return (
    <div className={styles.panel}>
      {room.dice.length === 4 && (
        <div className={styles.diceRow}>
          {room.dice.map((d, i) => (
            <div key={i} className={styles.die}>{d}</div>
          ))}
        </div>
      )}

      {combos.length > 0 && (
        <div className={styles.combos}>
          {combos.map((combo, idx) => {
            const playable = comboPlayable[idx]
            const selected = selectedCombo === idx
            return (
              <div
                key={idx}
                className={[
                  styles.comboCard,
                  selected ? styles.comboCardSelected : '',
                  !playable ? styles.comboCardDisabled : '',
                ].join(' ')}
                onClick={() => handleComboClick(idx)}
              >
                <div className={`${styles.comboSum}${selected ? ` ${styles.comboSumSelected}` : ''}`}>
                  {combo[0]} + {combo[1]}
                </div>
                <div className={styles.comboDetail}>
                  ({room.dice[0]}+{room.dice[idx === 0 ? 1 : idx === 1 ? 2 : 3]}) · (
                  {room.dice[idx === 0 ? 2 : idx === 1 ? 1 : 1]}+{room.dice[idx === 0 ? 3 : idx === 1 ? 3 : 2]})
                </div>
              </div>
            )
          })}
        </div>
      )}

      <div className={styles.btnRow}>
        <button
          className={`${styles.btn} ${styles.btnStop}`}
          onClick={onStop}
          disabled={climberCount === 0}
        >
          ✓ 베이스캠프
        </button>
        <button
          className={`${styles.btn} ${styles.btnRoll}`}
          onClick={handleRoll}
          disabled={room.rolledThisTurn && selectedCombo === null}
        >
          🎲 {room.rolledThisTurn ? '계속 굴리기' : '주사위 굴리기'}
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 3: 빌드 확인**

```bash
cd /Users/user/Runner && npm run build 2>&1 | tail -20
```

Expected: 에러 없음

- [ ] **Step 4: 커밋**

```bash
git add src/games/cant-stop/components/ActionPanel.tsx src/games/cant-stop/components/ActionPanel.module.css
git commit -m "feat(cant-stop): ActionPanel 컴포넌트 추가"
```

---

## Task 11: GameScreen

**Files:**
- Create: `src/games/cant-stop/screens/GameScreen.tsx`
- Create: `src/games/cant-stop/screens/GameScreen.module.css`

- [ ] **Step 1: CSS 작성**

```css
/* src/games/cant-stop/screens/GameScreen.module.css */
.screen {
  min-height: 100vh;
  background: #2c1810;
  display: flex; flex-direction: column;
  color: #f5e6c8;
  font-family: -apple-system, sans-serif;
}
.banner {
  background: #1a0e08;
  border-bottom: 2px solid #c8a45a;
  padding: 12px 16px;
  text-align: center;
  font-size: 15px; font-weight: bold;
  color: #c8a45a;
}
```

- [ ] **Step 2: 컴포넌트 작성**

```tsx
// src/games/cant-stop/screens/GameScreen.tsx
import { useCantStopGame } from '../hooks/useCantStopGame'
import MountainBoard from '../components/MountainBoard'
import ActionPanel from '../components/ActionPanel'
import type { PlayerKey } from '../types'
import styles from './GameScreen.module.css'

interface Props {
  roomId: string
  myKey: PlayerKey
}

export default function GameScreen({ roomId, myKey }: Props) {
  const {
    room, loading, isMyTurn,
    comboPlayable,
    handleRoll, handleSelectCombo, handleStop,
  } = useCantStopGame(roomId, myKey)

  if (loading || !room) {
    return <div style={{ color: '#a08060', padding: 24, textAlign: 'center' }}>연결 중...</div>
  }

  const climberCount = Object.keys(room.climbers ?? {}).length
  const bannerText = isMyTurn
    ? `⛰️ 내 차례 — 등반 중 (등반자 ${climberCount}/3)`
    : `상대방 차례`

  return (
    <div className={styles.screen}>
      <div className={styles.banner}>{bannerText}</div>
      <MountainBoard room={room} myKey={myKey} />
      <ActionPanel
        room={room}
        isMyTurn={isMyTurn}
        comboPlayable={comboPlayable}
        onRoll={handleRoll}
        onSelectCombo={handleSelectCombo}
        onStop={handleStop}
      />
    </div>
  )
}
```

- [ ] **Step 3: 빌드 확인**

```bash
cd /Users/user/Runner && npm run build 2>&1 | tail -20
```

Expected: 에러 없음

- [ ] **Step 4: 커밋**

```bash
git add src/games/cant-stop/screens/GameScreen.tsx src/games/cant-stop/screens/GameScreen.module.css
git commit -m "feat(cant-stop): GameScreen 추가"
```

---

## Task 12: ResultScreen

**Files:**
- Create: `src/games/cant-stop/screens/ResultScreen.tsx`
- Create: `src/games/cant-stop/screens/ResultScreen.module.css`

- [ ] **Step 1: CSS 작성**

```css
/* src/games/cant-stop/screens/ResultScreen.module.css */
.screen {
  min-height: 100vh;
  background: #2c1810;
  display: flex; align-items: center; justify-content: center;
  padding: 24px;
}
.card {
  background: #1a0e08;
  border: 2px solid #8a6a30;
  border-radius: 16px;
  padding: 40px 24px;
  width: 100%; max-width: 360px;
  display: flex; flex-direction: column; gap: 20px;
  text-align: center;
}
.resultTitle { font-size: 32px; font-weight: bold; }
.win  { color: #c8a45a; }
.lose { color: #a08060; }
.message { font-size: 16px; color: #f5e6c8; }
.playAgainBtn {
  background: #3a2010; border: 1.5px solid #c8a45a; border-radius: 10px;
  color: #c8a45a; font-size: 15px; font-weight: bold;
  padding: 12px 0; cursor: pointer;
}
```

- [ ] **Step 2: 컴포넌트 작성**

```tsx
// src/games/cant-stop/screens/ResultScreen.tsx
import type { CantStopRoomState, PlayerKey } from '../types'
import styles from './ResultScreen.module.css'

interface Props {
  room: CantStopRoomState
  myKey: PlayerKey
  onPlayAgain: () => void
}

export default function ResultScreen({ room, myKey, onPlayAgain }: Props) {
  const iWon = room.winner === myKey
  const myName = room.players[myKey]?.name ?? '나'
  const oppKey: PlayerKey = myKey === 'host' ? 'guest' : 'host'
  const oppName = room.players[oppKey]?.name ?? '상대'
  const winnerName = room.winner === myKey ? myName : oppName

  return (
    <div className={styles.screen}>
      <div className={styles.card}>
        <h1 className={`${styles.resultTitle} ${iWon ? styles.win : styles.lose}`}>
          {iWon ? '🏆 승리!' : '😔 패배'}
        </h1>
        <p className={styles.message}>
          {winnerName}님이 3개의 등반로를 완료했습니다!
        </p>
        <button className={styles.playAgainBtn} onClick={onPlayAgain}>
          처음으로
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 3: 커밋**

```bash
git add src/games/cant-stop/screens/ResultScreen.tsx src/games/cant-stop/screens/ResultScreen.module.css
git commit -m "feat(cant-stop): ResultScreen 추가"
```

---

## Task 13: CantStopApp + Registry 등록

**Files:**
- Create: `src/games/cant-stop/CantStopApp.tsx`
- Modify: `src/games/registry.ts`

- [ ] **Step 1: CantStopApp 작성**

```tsx
// src/games/cant-stop/CantStopApp.tsx
import { useState } from 'react'
import type { Session, PlayerKey } from './types'
import HomeScreen from './screens/HomeScreen'
import LobbyScreen from './screens/LobbyScreen'
import GameScreen from './screens/GameScreen'
import ResultScreen from './screens/ResultScreen'
import { subscribeRoom } from '../../shared/firebase/cantStopDb'
import { useRoom } from '../../shared/hooks/useRoom'
import type { CantStopRoomState } from './types'

export default function CantStopApp() {
  const [session, setSession] = useState<Session | null>(null)
  const { room, loading } = useRoom<CantStopRoomState>(
    session?.roomId ?? null,
    subscribeRoom
  )

  const handleEnterRoom = (roomId: string, myKey: PlayerKey, myName: string) => {
    setSession({ roomId, myKey, myName })
  }

  const handlePlayAgain = () => setSession(null)

  if (!session) {
    return <HomeScreen onEnterRoom={handleEnterRoom} />
  }

  if (loading || !room) {
    return <div style={{ color: '#a08060', padding: 24, textAlign: 'center' }}>연결 중...</div>
  }

  if (room.status === 'finished' && room.winner) {
    return (
      <ResultScreen
        room={room}
        myKey={session.myKey}
        onPlayAgain={handlePlayAgain}
      />
    )
  }

  if (room.status === 'playing') {
    return <GameScreen roomId={session.roomId} myKey={session.myKey} />
  }

  return <LobbyScreen room={room} roomId={session.roomId} myKey={session.myKey} />
}
```

- [ ] **Step 2: 게임 레지스트리에 등록**

`src/games/registry.ts`의 `GAMES` 배열에 추가:

```typescript
// 기존 import 아래 추가 (lazy는 이미 import됨)

// GAMES 배열에 추가:
{
  id: 'cant-stop',
  name: "Can't Stop",
  emoji: '⛰️',
  players: '1대1',
  component: lazy(() => import('./cant-stop/CantStopApp')),
},
```

- [ ] **Step 3: 최종 빌드 확인**

```bash
cd /Users/user/Runner && npm run build 2>&1 | tail -30
```

Expected: 에러 없음

- [ ] **Step 4: 테스트 전체 실행**

```bash
cd /Users/user/Runner && npm test
```

Expected: 모든 테스트 통과

- [ ] **Step 5: 커밋**

```bash
git add src/games/cant-stop/CantStopApp.tsx src/games/registry.ts
git commit -m "feat(cant-stop): CantStopApp 추가 및 레지스트리 등록"
```

---

## Self-Review

### 스펙 커버리지

| 스펙 요건 | 구현 태스크 |
|-----------|-------------|
| 1대1 고정 | Task 5 (createRoom/joinRoom: host/guest 2명) |
| 열별 칸 수 (2~12) | Task 2 (COLS 상수) |
| 주사위 4개 굴리기 | Task 3 (rollDice), Task 5 (rollDiceAction) |
| 조합 3가지 선택 | Task 3 (getDiceCombos), Task 10 (ActionPanel) |
| 등반자 최대 3개 | Task 3 (isComboPlayable: climberCount < 3) |
| 베이스캠프 확정 | Task 5 (stopClimbing) |
| 버스트 (선택 불가) | Task 5 (bust), Task 6 (hasPlayableCombo → 자동 bust 필요) |
| 승리 조건 (열 3개 점령) | Task 2 (isVictory), Task 5 (stopClimbing에서 호출) |
| 등반로 잠금 | Task 5 (stopClimbing: pos >= COLS[col] → locked) |
| UI: 보드 + 주사위 + 조합 카드 | Task 9, 10, 11 |
| 아이콘 (피켈/텐트) | Task 9 (MountainBoard 내 SVG) |

**발견된 갭: 버스트 자동 처리**

`hasPlayableCombo`가 `false`이고 주사위를 굴렸으면 자동으로 `bust()`를 호출해야 한다. `useCantStopGame` 훅에 `useEffect`를 추가해야 함. Task 6에 누락됨 → Task 6 수정:

`src/games/cant-stop/hooks/useCantStopGame.ts`에 아래 추가:

```typescript
// rollDiceAction 이후 자동 bust 처리
import { useEffect } from 'react'

// 훅 본문 내 추가
useEffect(() => {
  if (!roomId || !isMyTurn || !room) return
  if (!room.rolledThisTurn) return
  const combos = room.dice.length === 4 ? getDiceCombos(room.dice) : []
  const anyPlayable = combos.some(combo =>
    isComboPlayable(combo, room.board, room.climbers ?? {}, myKey)
  )
  if (!anyPlayable && combos.length > 0) {
    bust(roomId)
  }
}, [room?.rolledThisTurn, room?.dice, roomId, isMyTurn, myKey])
```

Task 6 Step 1의 훅 코드에 위 useEffect를 포함해야 한다. (Task 6 Step 1 코드 끝, `return { ... }` 직전에 삽입)

### Placeholder 스캔

- Task 10 ActionPanel의 `comboDetail` 계산이 복잡하고 인덱스 매핑이 부정확함. 단순화:

```tsx
// comboDetail 표시는 단순히 두 합계만 보여주는 것으로 수정
<div className={styles.comboDetail}>
  ({combo[0]}) + ({combo[1]})
</div>
```

### 타입 일관성

- `CantStopRoomState.climbers`는 `Record<string, number>` — 모든 태스크에서 일관하게 사용됨 ✓
- `PlayerKey = 'host' | 'guest'` — Task 1에서 정의, 이후 모든 파일에서 참조됨 ✓
- `ColumnState[PlayerKey]` 접근 패턴: `colState[myKey]` — TypeScript에서 `PlayerKey`는 `'host' | 'guest'`이고 `ColumnState`에 `host: number; guest: number`가 있으므로 인덱스 접근 가능 ✓
